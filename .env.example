# ===========================================
# Firebase Client (untuk frontend: login/register via Firebase Auth)
# Dapat dari: Firebase Console > Project Settings > General > Your apps > Web app
# ===========================================
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# ===========================================
# Firebase Admin (untuk API backend: Firestore, Auth)
# Opsi A (RECOMENDED): Pakai file JSON langsung - paling mudah
# ===========================================
# Simpan file JSON dari Firebase Console (Generate new private key) di folder project,
# lalu set path-nya (contoh: serviceAccountKey.json di root project):
GOOGLE_APPLICATION_CREDENTIALS=serviceAccountKey.json

# Opsi B: Variabel terpisah (jika Opsi A tidak dipakai)
# Dapat dari: Firebase Console > Project Settings > Service accounts > Generate new private key
# ===========================================
FIREBASE_ADMIN_PROJECT_ID=your_project_id
FIREBASE_ADMIN_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your_project_id.iam.gserviceaccount.com
# Paste persis dari JSON - HARUS dengan \n (backslash-n) untuk newline
FIREBASE_ADMIN_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIEvQIBADANB...\n-----END PRIVATE KEY-----\n"
